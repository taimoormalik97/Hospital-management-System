<%= form_for @bill, url: {action: :create} do |f| %> 
  <div class="container  pt-3"> 
      <div class="card">
        <div class="card-header p-3 container-fluid">
          <div class="row">
            <h3 class="col-md-9">
              <%= t('sales_order.add_order_label') %>
            </h3> 
          </div>
        </div> 
        <div class="card-body"> 
          <p><%= flash[:error] %></p>   
          <p>   
            <div class="form-group">   
              <label><%= t('sales_order.name_label') %></label>   
              <%= f.text_field :patient_id, :required => true, class: "form-control", id: "patient_search" %>   
            </div>
            <div class="form-group">   
              <label><%= t('sales_order.price_label') %></label>   
              <%= f.number_field :price, :required => true, class: "form-control" %>   
            </div>     
            <div class="form-group">   
              <label><%= t('sales_order.type_label') %></label>   
              <%= f.select :billable_type, options_for_select(['medicine', 'doctor']), selected: 'medicine', required: true %> 
            </div>    
          </p>
        </div> 
      </div>
      <div class="card-footer p-3 container-fluid">
        <div class="row d-flex justify-content-md-end">
          <div>   
            <%= link_to t('back'), { controller: 'bills', action: 'index'}, class: "btn btn-secondary" %>   
            <%= f.submit t('create'), class: "btn btn-primary"%>   
          </div> 
        </div>
      </div>    
    </div>    
  </div>
<% end %>     
