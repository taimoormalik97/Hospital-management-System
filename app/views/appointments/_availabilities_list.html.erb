<%= select_tag 'appointment[availability_id]', options_for_select(availability_list(params[:doctor_id], params[:date]).map { |availability| ["#{availability.start_slot.strftime("%I:%M%p")} - #{availability.end_slot.strftime("%I:%M%p")}", availability.id]}), {class: "form-control"} %> 
