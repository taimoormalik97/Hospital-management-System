<div class="container  pt-3"> 
  <div class="card">
    <div class="card-header p-3 container-fluid">
      <div class="row">
        <h3 class="col-md-10">Sales Orders</h3> 
        <div class="col-md-2 d-flex justify-content-md-end">
          <%= link_to 'Add Order', new_bill_path, class: "btn btn-primary" %>   
        </div>   
      </div>
    </div> 
    <div class="card-body">
      <table  class="table">   
        <thead >   
          <tr>   
            <th>Patient name</th>   
            <th>Type</th>    
            <th>Total bill</th>
            <th></th>  
          </tr>   
        </thead>   
        <tbody>   
        <% @bills.each do |bill| %>   
          <tr>   
            <td><%=link_to bill.patient.name, bill_path(bill)%></td>   
            <td><%= bill.billable_type%></td>
            <td><%= bill.price%></td>
            <div>   
              <td>
                <%= link_to 'Edit', edit_bill_path(bill), class: "btn btn-primary" %> 
                <%= link_to 'Delete', bill_path(bill), method: :delete, class: "btn btn-danger btn-spacing"%>      
              </td> 
            </div>       
          </tr>   
        <% end %>   
        </tbody>   
      </table> 
    </div>
  </div>
</div>
<div class="pagination justify-content-center" class="pagination-spacing">
  <%= will_paginate @bills, previous_label: t('paginate.previous'), next_label: t('paginate.next') %> 
</div>
