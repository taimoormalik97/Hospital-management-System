<%= select_tag 'appointment[availability_id]', options_for_select(availability_list(params[:doctor_id], params[:date], @current_hospital).map { |availability| ["#{convert_to_twelve_hour_format(availability.start_slot)} - #{convert_to_twelve_hour_format(availability.end_slot) }", availability.id]}), { class: "form-control" } %>
