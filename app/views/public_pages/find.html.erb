
<%= form_for @admin, url: check_email_path(@admin), method: "post"  do |f| %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", required: true %>
  </div>

  <div class="actions">
    <%= f.submit "Continue" %>
  </div>
<% end %>
