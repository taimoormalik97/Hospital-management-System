<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>
<%if @resource.confirmed_at.blank? && !@resource.admin? && @resource.reset_password_token.blank? %>
  <% @resource.class.confirm_by_token(@token) %>
  <p><%= link_to 'Confirm my account', edit_password_url(@resource, reset_password_token: @resource.set_reset_password_token, subdomain: @resource.hospital.sub_domain) %></p>
<%else%>
  <p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, subdomain: @resource.hospital.sub_domain) %></p>
<%end%>
